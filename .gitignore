# Secrets — never commit
.env
.env.*
# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
# SQLite DB (local dev data — don't ship real case data or user credentials in git)
*.db
*.db-journal
# IMAP ingestion output
incoming/
# OS cruft
.DS_Store
Thumbs.db
